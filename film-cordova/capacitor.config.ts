import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.oopsmails.ionic.angular.film-cordova',
  appName: 'film cordova',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
